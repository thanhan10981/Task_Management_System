import type { CloudinaryResourceType } from '../types/cloudinary.types';

export const CLOUDINARY_MAX_RESULTS = 500;

export const CLOUDINARY_RESOURCE_TYPES: CloudinaryResourceType[] = ['image', 'video', 'raw'];

export const FOLDER_MARKER_SUFFIXES = ['/.folder-marker', '/.folder-marker.txt'];
